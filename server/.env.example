# Example environment configuration for the unlinker backend.
# Copy this file to ".env" and fill in real values before running in production.

# HTTP port for the Express server
PORT=3000

# Ethereum JSON-RPC endpoint used by the escrow listener and sender
# e.g. from Infura, Alchemy, or your own node
CHAIN_RPC=https://mainnet.infura.io/v3/YOUR_INFURA_PROJECT_ID

# Deployed EscrowPool contract address
ESCROW_CONTRACT_ADDRESS=0xYourEscrowContractAddress

# Private key of the operator account that can withdraw from the escrow contract
# NEVER commit a real key to git; this is just a placeholder
OPERATOR_PRIVATE_KEY=0xYOUR_OPERATOR_PRIVATE_KEY

# Redis connection URL (e.g. from Upstash or another managed Redis)
# Upstash gives you a rediss:// URL like:
# rediss://default:LONG_TOKEN@YOUR_HOST:YOUR_PORT
REDIS_URL=rediss://default:YOUR_LONG_TOKEN@YOUR_HOST:YOUR_PORT